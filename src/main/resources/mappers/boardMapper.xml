<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">
	   <!-- 게시판 글 작성 -->
   	<insert id="insert">
	   INSERT INTO MP_BOARD(    BNO 
	   					      , TITLE 
	   					      , CONTENT 
	   					      , WRITER  )
	                 VALUES(    MP_BOARD_SEQ.NEXTVAL 
	                 		  , #{title}
	                 		  , #{content}
	                 		  , #{writer}  )
    </insert>
    
    <!-- 목록 게시판  -->
    <select id="list" resultType="com.project.vo.BoardVO">
		SELECT   BNO
			   , TITLE
			   , WRITER
			   , REGDATE
		  FROM MP_BOARD
		  ORDER BY BNO DESC
	</select>
	
    <!-- 조회 게시판 -->
	<select id="read" parameterType="int" resultType="com.project.vo.BoardVO">
		select		BNO
				 ,	TITLE
				 ,	CONTENT
				 ,	WRITER
				 ,	REGDATE
		FROM MP_BOARD
		WHERE BNO = #{bno}
	</select>
	
	<!-- 수정  -->
	<update id="update" parameterType="com.project.vo.BoardVO">
		UPDATE MP_BOARD
		   SET TITLE    =   #{title},
		   	   CONTENT  =   #{content}
		 WHERE BNO = #{bno} 
	</update>
	
	<!-- 삭제  -->
	<delete id="delete" parameterType="int">
		DELETE 
		  FROM MP_BOARD
		 WHERE BNO = #{bno}
	</delete>
	
</mapper>